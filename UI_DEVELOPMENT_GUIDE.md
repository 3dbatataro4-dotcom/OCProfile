# 介面功能開發須知

本文件適用於之後新增或修改的所有頁面、抽屜、視窗與互動功能。

## 不得在操作後跳回頂端

- 更新局部內容時，優先只更新必要的節點，不重新建立整個頁面。
- 如果必須重新渲染，重繪前記錄實際捲動容器的 `scrollTop`，完成後立即還原。
- 不得因按下保存、上傳、同步、篩選、選擇 Tag、切換排序或 AI 完成生成而把使用者送回頁首。
- 若重繪會改變項目高度，應以目前項目的 ID 或錨點恢復位置，而不是只依賴固定像素。
- 按鈕若位於表單內，非送出用途必須明確設定 `type="button"`，避免瀏覽器觸發表單提交及跳頁。
- 更新網址或返回紀錄時，避免使用會自動捲到頂端的導覽方式；必要時使用保留捲動位置的 history 狀態。

## 手機版面

- 以 320、360、390、430 像素寬度檢查，不得裁切最左或最右側操作按鈕。
- 空間不足時先縮小間距、內距與次要文字，再考慮橫向捲動。
- 重要按鈕仍需保留足夠點擊範圍，避免只縮小圖示而難以操作。
- 版本號、註釋、日期等長資訊應允許合理分行，不強迫擠在同一列。

## 每次功能完成前的檢查

1. 將頁面捲到中間或底部後執行新功能，確認位置不變。
2. 分別測試成功、失敗、載入中及取消操作。
3. 測試手機窄螢幕、日間模式與夜間模式。
4. 確認鍵盤焦點仍停留在合理位置，且返回鍵可以回到上一層。
5. 確認重新整理後資料與介面狀態符合功能設計。
