# 私訊與群組聊天

- 手機底部導覽為「廣場、發布、收藏、管理、私訊」。在其他版面點廣場會返回廣場；已在廣場或同人頁時再點一次，會開啟小抽屜，可切換全部貼文／純同人板塊。點外部或 Escape 可關閉。
- 點私訊可以查看對話列表與歷史訊息；點同好主頁的「私訊」也會開啟同一段對話。
- 「＋聯絡人」可加入工坊的人物卡或論壇虛擬用戶。角色使用加入當時的設定副本，可按「更新設定」手動同步；論壇用戶使用目前個性、喜好與簡短記憶。
- 每個直接對話綁定建立時的「我的帳號」；群組建立時可以選擇自己的聊天身分。舊版私訊會自動移入，舊訊息中未記錄的我的身分會標為「我（舊版紀錄）」。
- 「＋群組」可選擇最多 11 位聯絡人，加上自己的帳號，總數最多 12 人。群組內可混合角色本人與論壇同好；「成員」可重新調整名單與群組名稱，過去訊息會保留。
- 點「@ 成員」選擇對象，選單有名稱與 ID，可區分同名角色和同好。也可直接在文字中叫名字。選擇多位時，這些成員會各回覆一則；未指定時隨機選擇 1–3 位成員接話。
- 空白送出不會新增空白訊息，會讓聊天室成員接著對話或主動開話題。這只執行一輪，不會持續自動消耗 API。
- 角色以角色本人說話；論壇用戶以同好身分說話。每段對話只送出自己的最近 20 則訊息，不混用其他聊天室的內容。私人聊天不會自動發布公開貼文。
- 訊息顯示頭像、名稱、ID 與時間。舊紀錄先顯示最近 40 則，可載入更早訊息；生成中會顯示輸入動效，可停止；格式失敗可按重試，已送出的訊息不會重複新增。
- 不同對話的輸入草稿在目前頁面分開保留；已送出的聊天紀錄儲存於論壇存檔。重整會保留聊天紀錄，但不保留未送出的草稿。
- 聯絡人、對話與訊息支援論壇備份、挑選及合併。選擇一段對話匯出時會帶上其聯絡人、我的帳號與歷史訊息。

## 雲端升級

本版增加 `chatContacts`、`chats`、`chatMessages` 三個論壇資料集合。若已設定 Supabase，請重新執行最新 `supabase/setup.sql` 更新允許同步的欄位（不會清除既有資料）。聊天紀錄會包含在論壇雲端存檔中，AI 金鑰仍不包含在內。
